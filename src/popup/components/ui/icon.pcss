.icon {
    display: inline-block;
    vertical-align: middle;

    &--button,
    &--flag {
        width: 24px;
        height: 24px;
        flex-shrink: 0;
    }

    &--options {
        color: var(--brand-primary);
    }

    &--logo {
        width: 162px;
        height: 18px;
    }
}
